# This installs all packages that are used in the slides and code examples
# There are also many dependencies, which R will automatically install
install.packages(c("miscTools", "animation", "lattice", "rpart", "party",
                   "randomForest", "bst", "AUC", "pROC", "e1071", "klaR",
                   "ElemStatLearn", "glmnet", "Metrics", "fpc", "ggplot2",
                   "caret"))